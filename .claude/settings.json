{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); print(d.get('tool_input',{}).get('file_path',''))\" | { read -r f; [ -n \"$f\" ] && cd \"C:/Users/Usuario/OneDrive/Desktop/CLAUDIO/Fornno\" && git add \"$f\" && git commit -m \"auto: update $(basename \\\"$f\\\")\" && git push; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Commiteando y pusheando..."
          }
        ]
      }
    ]
  }
}
